package net.adoptium;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeIndexResourceIT extends IndexResourceTest {

    // Execute the same tests but in native mode.
}